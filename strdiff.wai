// compute the levenshtein distance of a given string against another.

union inputs {
    string, 
    list<string>
}

union outputs {
    u8,
    list<u8>
}

type error = string

lvd: func(a: inputs, b: inputs) -> expected<outputs, error>

